name: sastra_ebooks
description: Sastra E-Books

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.1.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^1.1.0
  shimmer: ^1.1.1
  rflutter_alert: ^1.0.3
  intl: ^0.16.1
  dio: ^3.0.9
  material_design_icons_flutter: ^4.0.5345
  flutter_svg: ^0.18.0
  path_provider: ^1.6.10
  animated_text_kit: ^2.2.0
  upi_india: ^2.1.2
  http: ^0.12.1
  url_launcher: ^5.4.10
  flutter_offline: ^0.3.0
  cached_network_image: ^2.2.0+1
  wiredash: ^0.2.0
  package_info: ^0.4.3
  flutter_icons: ^1.1.0
  font_awesome_flutter: ^8.8.1
  image_picker: ^0.6.7

  flutter_keyboard_size: ^0.1.2+2
  flutter_fullpdfview: ^1.1.3
  flutter_cache_manager: ^1.4.1
  outline_material_icons: ^0.1.1

  #firebase
  firebase: ^7.3.0
  firebase_storage: ^4.0.0
  firebase_core: ^0.5.0
  firebase_auth: ^0.18.0+1
  provider: ^4.1.2
  cloud_firestore: ^0.14.0+2
  firebase_messaging: ^7.0.0
  cupertino_icons: ^1.0.0
  flutter_keyboard_visibility: ^3.2.1
  workmanager: ^0.2.3
  keyboard_utils: ^1.2.4
  sticky_and_expandable_list: ^0.2.1
  expandable: ^4.1.4
  bubble_bottom_bar: ^1.2.0
  lokstory_flutter_lottie: ^0.0.4+10
  lottie: ^0.6.0
  flutter_upi: ^0.0.2
  # modal_bottom_sheet from the git repo
  modal_bottom_sheet:
    git:
      url: https://github.com/yahu1031/modal_bottom_sheet.git
  # keyboard visibility package getting from git
  keyboard_visibility:
    git:
      url: https://github.com/ton-An/flutter_keyboard_visibility.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/helpers/
    - assets/icons/
    - assets/lottie/

